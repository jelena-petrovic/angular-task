import { SenderValidatorDirective } from './sender-validator.directive';

describe('SenderValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new SenderValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
